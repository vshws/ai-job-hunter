services:
  - type: web
    name: ai-job-hunter
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python application_dashboard.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.1"
